name: Test Dotfiles
on:  push
jobs:  
  test:
    runs-on: macos-14
    steps:
      - name: Checkout Source
        uses: actions/checkout@v1
      
      - name: Run test
        run: |
          id #DEBUG
          chmod +x install.sh
          ./install.sh

{{/* Generated by `chezmoi init` on first setup of a new machine. */ -}}
mode = "symlink"

[edit]
  watch = true

[diff]
  exclude = ["scripts"]

[status]
  exclude = ["scripts"]

[data]
  # Apply macOS preferences (os/install.sh)? Default off; opt in here.
  macos = false
  # CI mode skips auth-dependent steps.
  ci = false

#!/usr/bin/env bash
# Install/update tmux plugins. Re-runs whenever .tmux.conf changes (hash below).
#
# tmux.conf hash:
# {{ output "sha256sum" (joinPath .chezmoi.workingTree "home" "dot_tmux.conf") }}
set -euo pipefail

TPM_INSTALL="${HOME}/.tmux/plugins/tpm/bin/install_plugins"
if [ -x "${TPM_INSTALL}" ]; then
  echo "==> Installing tmux plugins..."
  "${TPM_INSTALL}"
else
  echo "==> TPM not installed; skipping plugin install"
fi

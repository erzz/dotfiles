---
description: Set up an isolated git worktree lane for risky or parallel work.
agent: orchestrator
---

Load and follow the `worktrees` skill. Help the user set up an isolated git worktree for the task
at hand. Apply safety gates, document the lane's purpose, and prepare it for specialist delegation.

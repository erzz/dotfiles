# Rendered by mise from environment/fnox-provided secrets. Do not edit.
# Location: ~/.local/state/secrets.env (mode 0600)
# Refresh: render through mise after loading the appropriate secret environment.
# CI should use a separate environment that supplies these names; this template
# intentionally evaluates all four secret references unconditionally.
export GH_TOKEN={{ secret(name="GH_TOKEN") | quote }}
export ARTIFACTORY_AUTH_TOKEN={{ secret(name="ARTIFACTORY_AUTH_TOKEN") | quote }}
export NPM_TOKEN={{ secret(name="NPM_TOKEN") | quote }}
export LINEARB_API_KEY={{ secret(name="LINEARB_API_KEY") | quote }}
